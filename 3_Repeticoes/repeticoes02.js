const bolsa = [
   'notebook',
   'moleton',
   'chaves',
   'carregador',
   'Celular',
   'Mouse',
   'carteira'
]

for (let i = 0; i <=bolsa.length; i++) {
   console.log(bolsa[i])
   if (bolsa[i] == 'carregador') break
}